<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="customer_request_accounting_system.Views.NewEmployeePage"
             xmlns:viewModel="clr-namespace:customer_request_accounting_system.ViewModels"
             x:DataType="viewModel:NewEmployeeViewModel"
             Title="Добавление нового сотрудника">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="Margin" Value="0, 12,0,0"/>
            </Style>
            <Style TargetType="Entry">
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="Margin" Value="0,5,0,0"/>
            </Style>
            <Style TargetType="Editor" >
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Margin" Value="0,5,0,0"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="10" Spacing="3">
            <Label Text="ФИО" />
            <Entry Text="{Binding Name}" Margin="0,0,0,5"/>

            <Label Text="Номер телефона"/>
            <Entry Text="{Binding Phone}" Margin="0,0,0,5"/>
            
            <Label Text="Почта"/>
            <Entry Text="{Binding Email}" Margin="0,0,0,5"/>

            <Label Text="Отдел"/>
            <Picker SelectedItem="1" SelectedIndex="{Binding Department}" Margin="0,0,0,5">
                <Picker.Items>
                    <x:String>Тех. поддержка</x:String>
                </Picker.Items>
            </Picker>

            <Label Text="Должность"/>
            <Picker SelectedIndex="{Binding Post}" Margin="0,0,0,5">
                <Picker.Items>
                    <x:String>Младший специалист тех. поддержки</x:String>
                    <x:String>Старший специалист тех. поддержки</x:String>
                    <x:String>Администратор системы</x:String>
                    <x:String>Инженер тех. поддержки</x:String>
                    <x:String>Руководитель группы тех. поддержки</x:String>
                </Picker.Items>
            </Picker>

            <Label Text="Дата приема на работу"/>
            <Entry Text="{Binding DateOfEmployment}" Margin="0,0,0,5"/>

            <HorizontalStackLayout Padding="10" HorizontalOptions="Center" VerticalOptions="Center">
                <Button Text="Ок" Margin="10" Command="{Binding ClickOkButtonCommand}"/>
                <Button Text="Отмена" Margin="10" Command="{Binding ClickCancelButtonCommand}"/>
            </HorizontalStackLayout>
        </VerticalStackLayout>

    </ScrollView>
</ContentPage>